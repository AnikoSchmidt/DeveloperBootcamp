var concern = prompt("Are we there yet?");
// while (concern !== "yes" && concern !== "yeah") {
    while (concern.indexOf("yes") === -1) {
 var concern = prompt("Are we there yet? ");
}

alert("Yay, finally we made it!");
